name: php-angular
version: 0.0.1
inherits: wercker/php
type: service
platform: ubuntu@12.04
description: wercker box for php and angularjs
keywords:
  - angularjs
  - php

script: |
  sudo apt-get update
  sudo apt-get install nodejs
  sudo apt-get install npm
  sudo npm install -g karma
